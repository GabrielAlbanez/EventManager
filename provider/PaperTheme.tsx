import { DefaultTheme, Provider as PaperProvider } from 'react-native-paper';

export const customTheme = {
  ...DefaultTheme,
  custom: 'property',
  colors: {
    ...DefaultTheme.colors,
    
  },
};